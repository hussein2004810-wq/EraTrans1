# 🚀 خطوات تشغيل منصة KIUR من أي جهاز

ثلاثة مسارات مرتبة من الأسرع إلى الأكمل. نفّذ الأول الآن، والثاني عندما تريد بيانات مشتركة بين الأجهزة.

---

## المسار ١ — رابط عام مجاني خلال ٣ دقائق (Netlify Drop)

| # | الخطوة |
|---|--------|
| 1 | حمّل ملفات المشروع من بيئة العمل (المجلد المهم هو `dist` الناتج عن البناء الأخير) |
| 2 | افتح **https://app.netlify.com/drop** من أي متصفح — لا يحتاج تسجيلًا |
| 3 | اسحب مجلد **`dist`** وأفلته في الصفحة |
| 4 | انتظر 10–30 ثانية → ستظهر لوحة موقعك ورابط مثل: `https://kiur-abc123.netlify.app` |
| 5 | انسخ الرابط وافتحه من جوالك أو أي جهاز آخر ✅ |

> لتغيير اسم النطاق: بعد الرفع → Site settings → Change site name (مثال: `kiur-medical.netlify.app`).
> لتسجيل ملكية الموقع مجانًا: Add site → Claim site.

**البديل (Vercel):** https://vercel.com/new → Import مجلد المشروع → Framework: Vite → Deploy.

---

## المسار ٢ — توحيد البيانات بين كل الأجهزة (Supabase مجاني)

بدون هذه الخطوة: كل جهاز يحفظ حساباته ودرجاته محليًا ولا يراها الآخرون.
معها: أي طالب يسجّل من أي جهاز يظهر فورًا في سجل الطلاب عندك.

### أ. إنشاء المشروع السحابي (٥ دقائق)
| # | الخطوة |
|---|--------|
| 1 | افتح **https://supabase.com** → Start your project (بحساب GitHub مجاني) |
| 2 | **New project**: الاسم `kiur`، كلمة مرور قوية (احفظها)، أقرب منطقة (مثل Frankfurt) → Create |
| 3 | انتظر ~دقيقتين حتى تكتمل تهيئة المشروع |

### ب. إنشاء الجداول (دقيقة واحدة)
| # | الخطوة |
|---|--------|
| 4 | من القائمة اليسرى: **SQL Editor** → **New query** |
| 5 | انسخ كامل محتوى ملف **`supabase/schema.sql`** من المشروع والصقه |
| 6 | اضغط **Run** — يجب أن تظهر رسالة Success (ينشئ 11 جدولًا مع سياسات الأمان) |

### ج. ربط المنصة (دقيقة واحدة)
| # | الخطوة |
|---|--------|
| 7 | من Supabase: **Settings → API** → انسخ **Project URL** و **anon public key** |
| 8 | افتح المنصة وسجّل دخول **المالك**: `hussein2004810@gmail.com` / `kiur2024` |
| 9 | لوحة المالك ← تبويب **«المزامنة السحابية»** ← الصق القيمتين ← اضغط **«اتصال ومزامنة»** |
| 10 | ستظهر رسالة نجاح خضراء — البيانات المحلية بُذّرت للسحابة ✅ |

### التحقق
- سجّل حساب طالب جديد من **جهاز آخر** على نفس رابط Netlify.
- ارجع لجهازك → افتح لوحة المالك ← **سجل الطلاب** → سيظهر الطالب فورًا، ومع أي محاولة يؤديها ستصلك درجاته.

> **تحديث لاحق للمنصة؟** بعد أي `npm run build` وإعادة رفع `dist` على Netlify، اربط المزامنة مرة واحدة من تبويبها (القيم محفوظة محليًا على كل جهاز).

---

## المرحلة الثانية — مصادقة آمنة (كلمات مرور مشفّرة + حدّ المعدل)

ترفع مستوى الأمان: لا تُخزَّن أي كلمة مرور نصًا، وكل دخول يمر عبر Supabase Auth
(مشفّر bcrypt) مع حماية من التخمين المتكرر.

### أ. إنشاء دالة حدّ المعدل (SQL Editor)
| # | الخطوة |
|---|--------|
| 1 | SQL Editor → New query → انسخ محتوى **`supabase/migrations/20260103000000_password_rate_limit.sql`** → Run |
| 2 | فعّل الـ Hook: **Authentication → Hooks → Password Verification Attempt** → اختر الدالة `public.hook_password_verification_attempt` → Save |
| 3 | (مهم) عطّل تأكيد البريد: **Authentication → Sign In / Providers → Email** → أطفأ **Confirm email** حتى يعمل التسجيل فورًا |

### ب. إنشاء هوية المالك السحابية (تمهيد آمن)
أول حساب يُسجَّل في الوضع السحابي يصبح **مالك المنصة** تلقائيًا، لذلك:
| # | الخطوة |
|---|--------|
| 4 | على رابط المنصة المنشور: **سجّل الخروج** إن كنت داخلًا |
| 5 | من شاشة الدخول اختر **إنشاء حساب** وسجّل **ببريدك الحقيقي** (مثل `hussein2004810@gmail.com`) وكلمة مرور قوية |
| 6 | بما أن السحابة فارغة من الحسابات الحقيقية → ستصبح **مالك المنصة** فورًا ✅ |
| 7 | من لوحة المالك رقِّ المشرفين وامنحهم الصلاحيات والنطاقات كالمعتاد |

> حسابات العرض (owner@kiur.edu …) تعمل في الوضع المحلي فقط للتجربة، ولا تُرفع للسحابة.
> في الوضع السحابي استخدم بريدك الحقيقي — هو الأسلم والأوضح للطلبة.

### ملاحظة أمان اختيارية
إذا أردت منع الوصول المجهول تمامًا لجداول البيانات، شغّل يدويًا (وليس عبر db push) ملف:
`supabase/optional_authenticated_lockdown.sql` — واحرص أن تكون المصادقة السحابية مفعّلة أولًا.

---

## المسار ٣ — مستودع GitHub خاص + نشر تلقائي (اختياري)

```bash
# 1) ثبّت GitHub CLI من https://cli.github.com ثم سجّل:
gh auth login

# 2) من مجلد المشروع — ينشئ المستودع خاصًا ويرفع الكود بسطر واحد:
gh repo create kiur-platform --private --source=. --remote --push

# 3) نشر تلقائي: في Vercel → Add New Project → Import من GitHub
#    (الإعدادات تُكتشف تلقائيًا: Build = npm run build، Output = dist)
```
بعدها كل `git push` ينشر تحديثًا جديدًا تلقائيًا على رابطك.

---

## 🔐 تذكيرات قبل إرسال الرابط للطلبة

- غيّر كلمات المرور التجريبية من **سجل الحسابات** في لوحة المالك.
- الطلاب يسجّلون ذاتيًا: الجامعة ← الكلية ← القسم ← المرحلة.
- مشرفو الجامعات يراهم المالك فقط من تبويب **المشرفون والصلاحيات** (جامعة + قسم + صلاحيات).
- تصدير الدرجات: تبويب **تصدير الدرجات** → اختر اختبارًا أو الكل → Excel أو Word.

---

## ❓ حل سريع للمشاكل

| المشكلة | الحل |
|---------|------|
| الصفحة بيضاء بعد الرفع | تأكد أنك رفعت محتوى `dist` وليس مجلد المشروع كله في Netlify Drop |
| الطالب لا يظهر عندي | لم تربط المزامنة بعد → المسار ٢ |
| خطأ عند «اتصال ومزامنة» | تحقق من لصق Project URL كاملاً (يبدأ بـ `https://` وينتهي بـ `.supabase.co`) |
| أريد تغيير بيانات البذور | احذف جداول kiur من SQL Editor وأعد تنفيذ schema.sql ثم اربط مجددًا |
